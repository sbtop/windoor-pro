# Netlify configuration (fallback for deployment system)
# Primary deployment target: Vercel (see vercel.json)

[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"
  VITE_PWA = "true"
